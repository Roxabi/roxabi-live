# Exemptions — folder_size gate
# Format: <path>  # <N> files — <issue-url> <rationale>
# Gate disabled for node runtime (#180) — canonical script counts *.py only.
# Kept for /release-setup idempotency; no worker entries required.